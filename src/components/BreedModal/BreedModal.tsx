import React from "react";
import styles from './BreedModal.scss'

const BreedModal: React.FC = () => {
    // Maybe context for favourites as tis used across the app favourites and on image card open

    return (
        <>
            <div>BreedModal</div>
        </>
    );
};

export default BreedModal;